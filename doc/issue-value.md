# value()

Used in `lots`.

Reported: [#567](https://github.com/rustledger/rustledger/issues/567)

The `value()` function in Rust Ledger provides different results to the Beancount one, in `bean-query`.
